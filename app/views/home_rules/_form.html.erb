<%= form_for(home_rule) do |f| %>
  <% if home_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(home_rule.errors.count, "error") %> prohibited this home_rule from being saved:</h2>

      <ul>
      <% home_rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rule_id %>
    <%= f.number_field :rule_id %>
  </div>

  <div class="field">
    <%= f.label :home_id %>
    <%= f.number_field :home_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
