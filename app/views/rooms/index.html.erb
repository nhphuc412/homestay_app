<p id="notice"><%= notice %></p>

<h1>Rooms</h1>

<table>
  <thead>
    <tr>
      <th>Room type</th>
      <th>Bed type</th>
      <th>Bath type</th>
      <th>Accommodates</th>
      <th>Description</th>
      <th>Image</th>
      <th>Price</th>
      <th>Room name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= room.room_type_id %></td>
        <td><%= room.bed_type_id %></td>
        <td><%= room.bath_type_id %></td>
        <td><%= room.accommodates %></td>
        <td><%= room.description %></td>
        <td><%= room.image %></td>
        <td><%= room.price %></td>
        <td><%= room.room_name %></td>
        <td><%= link_to 'Show', room %></td>
        <td><%= link_to 'Edit', edit_room_path(room) %></td>
        <td><%= link_to 'Destroy', room, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Room', new_room_path %>
